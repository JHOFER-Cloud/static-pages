---
layout: base.njk
title: 404 - Not Found
permalink: 404.html
---
<main>
  <div class="box error-page">
    <div class="title-bar">
      <button aria-label="Close" class="close"></button>
      <h1 class="title">404 - Page Not Found</h1>
      <button aria-label="Resize" class="resize"></button>
    </div>
    <div class="error-content">
      <img 
        src="https://http.cat/404" 
        alt="HTTP 404 Cat" 
        class="http-cat"
        loading="eager"
      />
      <p class="error-message">
        Oops! This page doesn't seem to exist.
      </p>
      <p class="error-description">
        The page you're looking for might have been moved, deleted, or never existed in the first place.
      </p>
      <div class="error-actions">
        <button onclick="goBack()" class="back-button" id="backBtn">
          <svg class="button-icon" width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M8 2L2 8L8 14M2 8H14" stroke="currentColor" stroke-width="2" stroke-linecap="square" stroke-linejoin="miter"/>
          </svg>
          Go Back
        </button>
      </div>
    </div>
  </div>
  {% include "footer.njk" %}
</main>

<style>
  .error-page {
    margin-top: 2rem;
  }

  .error-content {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .http-cat {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    border: 3px solid var(--border);
    margin-bottom: 1.5rem;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  }

  .error-message {
    font-size: 1.3rem;
    font-weight: 500;
    color: var(--subtext);
    margin: 0 0 1rem 0;
  }

  .error-description {
    color: var(--subtext);
    margin: 0 0 2rem 0;
    max-width: 500px;
  }

  .error-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    width: 100%;
  }

  .back-button {
    padding: 0.75rem 1.5rem;
    font-size: 1rem;
    font-family: "MapleMono", monospace;
    border: 2px solid var(--border);
    background: var(--surface);
    color: var(--subtext);
    text-decoration: none;
    border-radius: 4px;
    transition: all 200ms ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    box-sizing: border-box;
  }

  .button-icon {
    flex-shrink: 0;
  }

  .back-button:hover {
    background: var(--overlay);
    color: var(--base);
    transform: translateY(-2px);
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
  }

  .back-button:active {
    transform: translateY(0);
  }

  .back-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .back-button:disabled:hover {
    background: var(--surface);
    color: var(--text);
    transform: none;
    box-shadow: none;
  }

  @media screen and (max-width: 768px) {
    .error-content {
      padding: 1.5rem;
    }

    .error-message {
      font-size: 1.1rem;
    }

    .error-description {
      font-size: 0.9rem;
    }

    .http-cat {
      margin-bottom: 1rem;
    }

    .error-actions {
      flex-direction: column;
      width: 100%;
    }

    .back-button {
      width: 100%;
      justify-content: center;
    }
  }

  @media screen and (max-width: 480px) {
    .error-content {
      padding: 1rem;
    }

    .error-message {
      font-size: 1rem;
    }

    .back-button {
      padding: 0.6rem 1.2rem;
      font-size: 0.9rem;
    }
  }
</style>

<script>
  function goBack() {
    // Check if there's history to go back to
    if (window.history.length > 1) {
      window.history.back();
    } else {
      // If no history (e.g., direct link), go to homepage
      window.location.href = '/';
    }
  }

  // Disable back button if there's no history
  window.addEventListener('DOMContentLoaded', function() {
    const backBtn = document.getElementById('backBtn');
    if (window.history.length <= 1) {
      backBtn.disabled = true;
      backBtn.textContent = '← No Previous Page';
    }
  });
</script>
